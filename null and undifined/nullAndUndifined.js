// ----------------------------------------------------------------------

let msg;

console.log(msg, typeof(msg));

msg = msg + 20;

console.log(msg, typeof(msg));

// ----------------------------------------------------------------------

let msg2 = null;

console.log(msg2, typeof(msg2));

msg2 = msg2 + 20;

console.log(msg2, typeof(msg2));

// ----------------------------------------------------------------------




